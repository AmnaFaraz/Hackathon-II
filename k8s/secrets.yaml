apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: todo-app
type: Opaque
# Replace the value with your base64 encoded strings
# echo -n "your-value" | base64
data:
  DATABASE_URL: REPLACE_WITH_BASE64_DATABASE_URL
  GROQ_API_KEY: REPLACE_WITH_BASE64_GROQ_API_KEY
